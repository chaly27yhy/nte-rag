# 运行时依赖（会被 PyInstaller 打包进 exe）
# 刻意不引入 torch / sentence-transformers / jieba：
#   - torch 会让包体从 ~60MB 涨到 1.5GB+
#   - jieba 只有源码包（需构建隔离），且中文检索用内置 bigram 分词已足够
# 中文分词由 app/core/chunk.py 实现（汉字 bigram + 英文单词 + 数字）；
# jieba 是软依赖：运行环境里恰好有就自动启用为增强，没有也照常工作。

fastapi>=0.115
# app/server/api.py 直接 import，不靠 FastAPI 的传递依赖兜底
pydantic>=2.9
starlette>=0.40
uvicorn>=0.32
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.3
trafilatura>=1.12
ddgs>=9.0
pywebview>=5.3
