# 安全策略（SECURITY）

本文件说明这个项目的安全边界、哪些情况不算漏洞，以及怎么私下报告漏洞。

---

## 1. 支持的版本

| 版本 | 是否维护 |
|---|---|
| 最新的 `1.0.x` 发行版 | ✅ 接受安全报告，会出补丁版本 |
| 更早的 `1.0.x` / 任何预发行构建 | ❌ 不再单独修，请先升级到最新版 |
| 从源码自行修改后构建的 exe | ❌ 无法判断你改了什么，请先用官方发行版复现 |

以源码运行（`.venv\Scripts\python.exe run.py`）与发行版 exe 的行为基本一致。如果问题只在你自己改过的代码上复现，不属于这里。

---

## 2. 威胁模型

它是什么：一个单机、单用户的 Windows 桌面工具。进程内跑一个 FastAPI 服务，
用 pywebview（WebView2）开一个窗口显示本地 HTML 界面，知识库是本地 SQLite + FTS5。

它不是什么：不是服务端产品，没有账号体系，没有云端组件，也没有遥测。

具体的边界：

1. 只绑定 `127.0.0.1`：服务只监听回环地址的随机端口，不对局域网或外网开放。
   也不接受把服务放到 0.0.0.0 后面当共享服务用：那不是它的设计场景，
   也不在加固范围内。
2. 一次性会话令牌：启动时生成 `secrets.token_urlsafe(24)`，只在 `GET /` 时下发给浏览器，
   写进 `HttpOnly` + `SameSite=Strict` Cookie。其余请求都要带令牌，比较用
   `secrets.compare_digest`，常量时间。另有 Host 白名单拒绝非本机 Host 头，防 DNS rebinding。
3. 无服务端、无遥测：程序不会把你的问题、知识库、配置上传到本项目维护者的任何服务器。
   唯一的外发请求有三类：你自己配置的模型 API、你自己配置或免 Key 兜底的搜索接口，
   以及为了补齐知识库而抓取的目标网页。程序里没有分析、没有埋点，也没有自更新，
   不会下载或执行任何新代码。
4. 数据都在你的磁盘上：知识库 `knowledge.db`、配置 `config.json`、日志都在
   `%APPDATA%\NTE-RAG`。便携模式下在 `data\`，也就是 exe 同目录放了 `portable.flag`，
   或该目录可写的时候。卸载就是删掉这个目录。

---

## 3. API Key 是怎么保护的（以及它保护不了什么）

- 用户自己填的模型 / 搜索 Key 用 Windows DPAPI（`CryptProtectData`）加密后写入 `config.json`，
  相关代码在 `app/core/secrets.py`。
- DPAPI 的密钥材料绑定当前 Windows 账户。所以换机器或换 Windows 账户之后，`config.json`
  解不开，程序会把它当作「没有 Key」，提示你重新填写。这是预期行为，不是缺陷，
  也不要把它当加密 bug 上报。想跨机携带 Key，只能重新填一次。
- 界面与接口只回显掩码，也就是前 4 位 + 后 4 位。`config.json` 与日志写在 `%APPDATA%\NTE-RAG` 下，
  使用受限权限，不外发也不共享。
- 日志、错误信息与堆栈在落盘前会先经过 `secrets.scrub()`，把密钥形状的串替换成掩码。

---

## 4. 已知的非目标与接受的风险

下面这些属于已知问题，不在修复范围内，报上来会按「已知情况」处理：

1. 你自己把 Key 贴出去。日志会脱敏，但你在 issue、聊天、截图、录屏里主动展示 Key，
   或者把 `config.json` / `data\` 目录打包发给别人，都不在保护范围内。截图前请确认设置页上显示的是掩码。
2. 抓到的第三方网页文本是不可信输入。知识库里的内容来自公开 wiki / 官网 / 社区站点，
   程序会做清洗、跨源一致性与可信度打分，但不能保证抓到的页面没有恶意内容。
   提示词层面做了接地检验，回答必须落在检索到的原文上，见 `app/core/rag.py` 的 `_is_grounded`。
   这降低了提示词注入的影响面，但不构成对「网页内容可信」的保证。
3. 本机其它进程 / 本机管理员。保护的是「远程攻击者」与「同机其它网页」，不是
   「已经拿到你 Windows 账户的同机代码」。DPAPI 与文件权限都拦不住后者。
4. 依赖上游的漏洞。FastAPI、uvicorn、httpx、lxml、trafilatura、pywebview 等第三方库
   各自的漏洞请按上游渠道报告；如果它在本项目的使用方式下可被触发，也欢迎向本仓库报告。
5. 模型的输出。模型可能答错，这是知识工具的固有问题。程序靠引用来源、可信度派生与
   「没依据就明说不知道」来缓解，见 [`docs/architecture.md`](docs/architecture.md) §4；
   但答错本身不是安全漏洞。

---

## 5. 如何私下报告一个漏洞

请不要为可利用的安全问题开公开 issue。

首选方式是仓库的 GitHub Security Advisories：在仓库页面进 Security → Advisories → Report a vulnerability，
以私密公告的形式提交，只有维护者能看到。如果这个入口不可用，再退一步，
用仓库的 issue 追踪器发一条不含利用细节的说明，请维护者开一个私密渠道。
不要在公开 issue 里贴 PoC、Key、日志原文或可复现的利用步骤。

也请不要用「猜联系方式」的方式上报，本仓库只保证上面这两条渠道。

### 5.1 报告里请包含

- 受影响的版本（`run.py --version` 的输出，或窗口标题栏的版本号）与 Windows 版本；
- 问题类型与影响：能读到什么、能改到什么、需要什么前提条件；
- 复现步骤，尽量最小化、按序、可照做；如果依赖特定配置，比如开了便携模式或改过端口，请写明；
- 需要的证据：脱敏后的日志片段、请求/响应示例。请自行把 Key 打码；
- 如果已有修复思路，欢迎一并给出。

### 5.2 承诺什么、不承诺什么

- 尽力而为，没有 SLA。这个项目没有公司、没有值班团队、没有赏金计划，是维护者在业余时间做的。
  请按「一位愿意修的志愿者」来预期响应时间，而不是按「一个厂商」。
- 会做的：确认收到 → 判断是否在范围内 → 在私密公告里同步进展 → 修好后在
  `CHANGELOG.md` 与发布说明（如果合适）里记录，并在征得你同意的前提下致谢。
- 不会做的：为了赶时间改数据标准或跳过自检。`tools/quality_check.py` 必须全绿。

---

## 6. 给贡献者的一条硬约定

`requirements-dev.txt` 及其引入的东西，包括 PyInstaller、Pillow 和构建/测试辅助脚本，
仅供开发与打包使用，`app/` 下的任何代码都不得 import 它们。
这样 exe 里就不会出现只需要在构建机上存在的代码，攻击面与体积都保持最小。
如果某个能力需要在运行期用到，请把它作为软依赖处理：
导入失败就降级并记录原因，不要把构建期依赖变成运行期依赖。
