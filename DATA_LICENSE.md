# 内置资料的授权与移除说明（DATA LICENSE）

本仓库的**程序代码**采用 MIT 协议，见 `LICENSE`。

下面这些内容不在 MIT 授权范围内，它们的著作权归第三方：

- `seed/seed_kb.json`：随程序分发的内置种子知识库；
- `eval/*.json` 中引用的第三方内容摘要，题目本身是项目原创，见第 6 节；
- `docs/` 下文档里引用的第三方页面片段。

如果你只想用代码，第 1 节之后的内容可以跳过。删掉 `seed/seed_kb.json` 程序照常能跑，
只是首次运行不再有种子可导入，会落到空库 + 联网抓取。

---

## 1. 内置资料的性质

> 内置种子库里只有**文本**，没有美术素材、没有官方配图、没有可执行内容。
> 文本有两类：685 条由原文摘要出的结构化事实（带来源 URL），以及 152 段原文摘录。
> 这些文本的权利归原站点与原作者所有，本项目不主张任何权利。

`seed/seed_kb.json` 自己的 `note` 字段写的就是「仅含服务端渲染页面与公开 API 的正文」，
下面逐项展开。

## 2. 收录了什么（实测数字，可复核）

快照时间 `2026-09-22T22:49:06`（`seed_kb.json` 的 `generated_at`），由 `tools/seed_builder.py` 生成。

| 组成部分 | 数量 | 体积/长度 | 性质 |
|---|---|---|---|
| 文档（`documents[]`） | 70 篇 | — | 只存 URL、标题、时间、内容哈希与元信息，**不存整页 HTML** |
| 原文摘录（`documents[].chunks`） | 152 段 | 合计 77,294 字符；单段 120–700 字符（中位 636） | 正文的**连续摘录**，见下方说明 |
| 结构化事实（`facts[]`） | 685 条 | `answer` 单条 9–401 字符（中位 22） | 摘要式短句，每条都带 `source_url` |

摘录是怎么切的：分块按约 700 字符切分。对短页面来说，比如一条公告、一段角色简介，
全部切片接起来可能接近该页全文。所以不能把它说成「只是索引」，
能保证的只有三件事：不含图片与美术素材、不含整页 HTML 存档、每一条都能回溯到原始 URL。

出处分布（`facts[]` 条数 / `chunks` 段数，按域名；下表「事实」列只数 `facts[]`，合计 685 条，
70 篇 `documents[]` 不计入这一列）：

| 域名 | 事实 | 摘录段 | 项目标注的来源类型 |
|---|---|---|---|
| `wiki.biligame.com/yh`（BWIKI） | 472 | 66 | `wiki` |
| `m.wywyx.com`（玩一玩） | 104 | 0 | `wiki`（第二来源） |
| `yh.wanmei.com`（官方网站） | 38 | 33 | `official` |
| `shouyou.3dmgame.com`（3DM） | 31 | 11 | `community` |
| `www.gamersky.com`（游民星空） | 30 | 10 | `community` |
| `zh.moegirl.org.cn`（萌娘百科） | 8 | 32 | `wiki` |
| `kamigame.jp` | 1 | 0 | `manual`（人工录入） |
| `news.17173.com` | 1 | 0 | `manual`（人工录入） |

这 685 条里有 2 条是人工录入的（`tools/manual_facts.py`，`source_type=manual`，就是上表最后两行），
其余由抓取结果摘要而来；两条人工条目的来源同样逐条标注。

## 3. 使用方式

- 只在**本地**用：程序把种子导入本机 SQLite，检索与生成都在用户机器上完成。
- 只用于**检索、引用与跨来源核对**（同一 (实体, 字段) 被哪几个独立域名写过、值是否一致）。
- 每条结论都标注 `source_url`；界面上的来源链接直接指向原站点。
- 不随程序提供任何在线服务，不把内容再分发给第三方，不做内容聚合站点。
- 不含任何官方美术资源：界面图标全部由代码绘制（`app/web/` 内无图片素材，
  唯一的图标文件 `assets/icon.ico` 也是脚本生成的）。

## 4. 权利方如何要求移除

不需要法律文书，任选一种方式：

1. 提 Issue，用模板 [`.github/ISSUE_TEMPLATE/takedown.md`](.github/ISSUE_TEMPLATE/takedown.md)（写明站点 + 条目标题/URL，能定位到条目即可）；
2. 或直接联系仓库维护者（见仓库主页）。

承诺如下：

- 收到请求后**立即**从 `seed/seed_kb.json` 删除对应文档与事实（含由它派生的条目），
  并把移除记录写进 `docs/consistency_review.md`；
- 在下一次发布中生效；已发布的历史版本无法追回，但会尽快用新版本替代；
- 对归属有争议的内容，**默认可先移除、后讨论**。

重建与删除的具体位置：`tools/data_audit.py`（体检）、`tools/manual_facts.py`（人工录入层）、
`tools/seed_builder.py`（重建种子）、`tools/dedupe_report.py`（合并重复）。

## 5. 各站点归属

《异环》（Neverness to Everness）的名称、角色与相关素材的权利归其开发商与发行商所有。
上表各站点的内容权利归各站点与原作者所有。本项目是**玩家自制的非官方工具**，
与官方团队及上述任何站点**没有隶属、合作或授权关系**。

各站点的逐条归属、评测集与上游依赖的许可清单另见 `THIRD_PARTY_NOTICES.md`。
代码本身采用 MIT 协议，正文见 `LICENSE`（那份文件里只有标准 MIT 条款，不再夹带说明）。

## 6. 不属于第三方内容的部分

- `app/`、`tools/` 下的全部程序代码：MIT。
- `eval/` 下的**题目、评分标准与脚本**：项目原创，MIT。
  其中的参考答案引用了上述站点的内容摘要，这部分按本文件处理。
- `docs/` 下的架构、审计与开发记录：项目原创（MIT），其中引用的第三方片段同理。
- `eval/report-*.json`：运行产物，不入库（`.gitignore` 已排除）。

## 7. 为什么明知有授权风险还是内置

离线可用是这个工具的核心体验：玩家在游戏崩溃、断网或官方站点改版时依然能查到资料，
联网抓取只作为补充。代价是必须把一部分第三方文本随包分发。所以这里要把
「收了多少、怎么用、怎么删」写清楚，而不是含糊地说一句「仅供学习交流」。

如果你是要 fork 这个项目的人：请自行判断你所在司法辖区的合规性，
`seed/seed_kb.json` 可以直接删掉，程序不会因此无法启动。
