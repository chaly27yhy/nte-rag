# 更新日志

本文件记录本项目的重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-24

首个公开版本。下面「新增 / 变更 / 修复」里的内容都是开发期内完成的，包括发布前发现并修掉的问题，
全部包含在首个版本里。

### 新增

- Windows 单文件绿色版：PyInstaller 打包，免安装，解压即用。
- 本地 FastAPI 服务 + pywebview（WebView2）窗口界面；前端为原生 HTML/CSS/JS，无构建链。
- SQLite + FTS5 本地知识库，内置种子库：70 篇文档 / 152 分块 / 685 条结构化事实（2026-09 快照）。
- 抓取多个公开数据源，并做跨来源一致性裁决：冲突保留，人工裁决留档。
- 四级可信度评分：来源等级 0.45 + 提取方式 0.25 + 跨源一致性 0.20 + 时效 0.10。
- 用户自填的模型 Key 使用 Windows DPAPI 加密保存，仅回显掩码。
- 资料不足时自动联网抓取，并将结果写回本地知识库。
- 698 项机制自检（`tools/quality_check.py`，不需要模型也不需要网络；干净检出会跳过依赖本地产物的部分，只要求「失败 0」），以及离线评测集 `eval/`。

### 变更

- 程序内「关于」页扩写成可以单独当概览读，不读 README 也能了解项目：新增「这个程序能做什么」卡片
  （五个页签各自能干什么、内置资料规模）与「授权与许可」卡片（代码 MIT、内置资料走
  `DATA_LICENSE.md`、依赖许可与来源署名见 `THIRD_PARTY_NOTICES.md`、不含官方美术素材），
  抓取那条补上预置黑名单与主题词闸门，AI 说明去掉括号夹注；另补「关于这个项目」卡片
  （主要方向是自带大模型 API、作者单人维护的第一个开源项目、Windows 专用且资料只有中文、
  开发过程用了 AI 编程助手、欢迎反馈）与「资料会过期」卡片（2026-09 快照、可信度是算出来的
  而不是模型自评、抓不到页面会报错不会编答案、版本与数值以游戏内和官方公告为准），
  免责卡片里给出反馈与移除申请的入口。
- 设置页标题原本写「模型 API（必填）」，但没配模型时程序会退化为「本地资料直出」，并非不可用，
  现在改叫「可选」，`README.md` 同步。
- README 只保留用户使用部分。环境搭建、运行调试、诊断工具、重建种子库、打包与发布、
  平台适配、项目结构与入口文件说明，以及全部踩坑记录，统一搬到
  [`docs/development.md`](docs/development.md)；README 留一节「开发速查」与产物哈希校验。
  `README.en.md` 同步改为指向该文，并注明深入内容仅中文。同时把 `## 给最终用户怎么用`
  改名为 `## 使用说明`。
- `GET /api/health` 现在只回 `{"ok": true}`。它不鉴权，启动等待与自检要用，那时还没有令牌；
  本机任何进程、任何网页都能读到，因此不再顺带暴露程序版本与知识库后端形态。
  版本、知识库统计与 FTS 状态改由已鉴权的 `GET /api/state` 提供，界面读的也正是那里。
- 知识库结构版本守卫：库里记录的 `schema_version` 比当前程序新时拒绝打开，并提示升级程序，
  避免新版程序建的库被旧版程序写坏。旧库照常自动升级。
- 事实检索的 FTS 查询失败、或返回零行时自动退回 LIKE 兜底，一条带引号的查询不会再让检索报错。
- `sim_hash` 近邻预筛字段改为分批回填，老知识库第一次打开时不再一次性全表更新。
- `POST /api/config` 对越界数值直接夹紧，包括 `top_k`、`chunk_size`、`temperature` 等，
  不再把非法值原样落盘。
- 开发用 `.env` 需要两道闸门：非打包运行 + 显式 `NTE_RAG_DEV_ENV=1`，且只填空着的密钥字段，
  不会覆盖已保存的 Key。
- `LICENSE` 原先把 MIT 正文和一段中文的「内置资料说明」拼在同一个文件里，GitHub 的许可识别器
  读到非标准内容就放弃，仓库侧边栏一直显示 `NOASSERTION`；现在该文件只有标准 MIT 条款，
  那段说明整体搬进 `DATA_LICENSE.md`（`README.md`、关于页和 Release 说明本来就指向它）。
- `docs/` 从 13 份精简到 5 份：审计报告、优化方案、开发流水这类「当时怎么修的」过程记录不再随
  仓库分发，只留架构、开发、文档索引、数据来源与一致性判例；发布文案写完即随 Release 一起
  发布，不再留在仓库里（`docs/release_notes.md` 已删），[`docs/README.md`](docs/README.md)
  相应重写。
- 统一仓库行尾：`app/web/app.js`、`seed/seed_kb.json` 等 8 个文件的 CRLF 改成 LF。种子文件的
  字节随之变化，指纹对不上的库会在启动时重导一次种子资料（内容不变）；发布产物一并重建，
  `README.md` 与 `docs/development.md` 里的哈希同步更新。
- [`docs/consistency_review.md`](docs/consistency_review.md) 改成面向读者的说明文档：去掉批次编号、
  复验流水与内部变更记录，保留 14 条多源一致判例、8 行 wikitext 证据表、撤回原因与三条裁定。

### 修复

- 首次推送后 GitHub Actions 直接判 `Invalid workflow file: Unrecognized named-value: 'runner'`，
  两次推送各留下一条「一个 job 都没有」的失败记录。原因是工作流顶层的 `env` 里写了
  `NTE_RAG_DATA_DIR: ${{ runner.temp }}\nte-rag-ci`，而 `runner` 上下文在工作流级的 `env` 里
  不可用——本地怎么跑都看不出问题。数据目录改成在自检步骤里 `$env:NTE_RAG_DATA_DIR = $dataDir`
  设置，并新增自检【35】钉住「工作流级只写字面量」，免得以后又被静默拒收。

- 发布前补齐中文入口：中文 `README.md` 加了「1. 下载」与前置条件表（原来只有英文 README 写了去哪拿 exe，
  中文读者看不到下载入口）。几处文档与实现不一致也一并改掉：`README.md` 里
  「设置 → 抓取节奏」这个界面控件并不存在（改成 `config.json` 的 `fetch.cooldown_seconds`）、
  可信度算式漏了手写来源的提取方式 `manual 0.95`、向导四步写错、英文 README 说「你不点就不联网」
  与实际默认自动联网相反、文档里两个 `docs/` 相对链接指到了仓库根。程序侧修掉「关于」页设置项说明里
  `**不预填默认值**` 的星号会原样显示在界面上的问题，`requirements.txt` 补上 `app/server/api.py`
  直接 import 的 `pydantic` 与 `starlette`，`.gitignore` 补 `*.db` / `*.sqlite*`，
  `tools/verify_exe.ps1` 增加一项：发行目录里不许有数据库 / 配置 / 便携标记（解压即用）。

- 设置页「拉取可用模型」拉不到模型，而首启向导里同样的配置能拉到。原因是「测试连接 /
  拉取可用模型」共用的那个探针把请求体里的 Base URL 一律丢掉，只认服务端预设表的地址或
  上一次保存的地址；向导每一步都先保存，所以它的请求正好打在自己刚存下的地址上，
  设置页是「填完就点」，请求打到了旧地址，用户只看到上游一句莫名其妙的报错。
  顺带查出一层：这四个接口界面发的是**扁平**的 section 字段，而探针按 `payload[section]`
  读，等于整份请求体都没读到，探针一直在拿旧配置探测。现在探针两种形态都认，
  地址的采用规则是：带新密钥就采用它带来的地址（密钥是请求方自己给的，没有外泄路径）；
  不带新密钥却要换主机（含只换服务商）时明确报「需要重新填写 API Key」而不是悄悄打旧地址，
  也不把已存的密钥送到新主机；没有旧密钥可泄、或主机没变时照用请求里的地址。
  掩码串不算新密钥，判据与 `Config.set_secret` 共用 `secrets.is_mask_value()`，
  免得出现「提示重填 Key、填了同样的 Key 仍被拒」。探针改成模块级函数并只动一次性副本，
  活配置的内存与磁盘一个字节都不变；「拉取可用模型」失败时会把程序实际请求的地址一起显示出来，
  地址没生效时用户一眼就能看出打的是哪里。还有一处前后矛盾：程序自己发的「更换模型服务地址后
  需要重新填写 API Key」这句，原先会被前端的错误归类改写成「密钥无效或无权限」，用户以为 Key
  打错了，实际要做的只是重新粘贴一次；现在这句有专门的归类与提示，判定顺序也被断言钉住
  （回归断言：`tools/quality_check.py`【34】）。

- 助手对「薄荷的生日是哪天？」答「现有资料里出现了两个互相矛盾的说法：6月1日 / 8月20日」。
  查下来，8月20日 不是库里的条目，而是 BWIKI 薄荷角色页的**文档切片**：那一页整页伪造
  （技能整段抄自《银与血》角色「莱夏」的页、人物故事抄自本站「早雾」页、生日与 CV 与本站
  「娜娜莉」页相同，2026-09-22 审计已记录），可它在库里仍是正常文档，检索层照样把它的切片
  当证据送进上下文，跟人工裁定过的「6月1日」撞车，模型只能回答「无法判断哪个为准」。
  现在按页面撤回：名单写在 `app/core/curation.py`，命中后该页的文档、切片与条目置为
  `revoked`（软撤回，数据保留，复核结论有处可查），检索层只认 active/conflict 因而看不到它；
  写入侧与种子导入侧各拦一道，手动添加同样拒收。已有库在启动时按名单指纹补跑一次迁移，
  指纹没变就跳过，撤回出错则不写指纹、下次启动重试。
  种子文件本身不动：改了它会改变所有用户的 `seed_fingerprint`、触发整份重导
  （回归断言：`tools/quality_check.py`【33】）。
  【18】原来那份「冲突行不许被种子导出丢弃」的端到端测试用的是 BWIKI 薄荷页的真实地址，
  现在那一页会被撤回名单拦下，于是该测试改用中性域名、只测冲突行本身，另加一条断言确认
  同一个地址在种子导入这条路上也会被跳过。
- 更新报告的计数口径自相矛盾：一轮日志写「抓取 17 页，过滤低质量页面 1 页，8 条错误」，
  但同一轮实际只碰到 15 个不同页面，8 条里还有 5 条是按 robots 规则主动跳过、并非失败。
  原因有三个：主动跳过被算成「错误」、被过滤的页面同时进「过滤」与「错误」、
  内容没变的重抓在判断之前就计进了「新页面」。现在报告拆成「取到正文 / 新页面（+ 内容未变）/
  按 robots、黑名单规则跳过 / 过滤 / 抓取失败 / 错误」六个计数，且
  `取到正文 = 新页面 + 过滤 + 内容未变`；错误明细不再每个主题只留前 3 条，
  更新页的状态卡也改成同一套说法（回归断言：【32】）。
- 官网此前每轮都被跳过，理由是「robots.txt 不存在，允许抓取」——却被记成一条错误。
  `allowed()` 只认 `ok` 状态，而 `missing`（`robots.txt` 404，或 HTTP 200 但内容为空，
  官网正属后者）一并被判成「不允许抓取」。按 RFC 9309，没有规则就是不限制：现在
  `missing` 允许抓取，只有**读不到**（超时 / 5xx / 网络错误）才继续保守拒绝
  （回归断言：`tools/quality_check.py`【31】）。
- 「立即更新」会抓回与游戏无关的页面：实测「异环 地图 区域 探索」抓回的是**单个汉字
  「异」的字典页**（百度百科、汉语国学、汉语查、新华字典），另外存进 4 个 B 站视频页——
  正文只有标题与推荐列表。现在有两道新闸门：预置黑名单拦掉字词/字典站与视频页
  （百度百科整站保留，那里有正经的《异环》词条），社区来源的页面要求标题或正文里
  出现主题词（默认 5 个主题的交集＝「异环」；官方站与 wiki 站不适用，主题交集为空时
  闸门自动失效）。用户自己粘的地址按用户意愿处理，不受预置黑名单影响。
- 首启向导的界面冻结：向导打开时给 `#app` 设 `inert`，而向导自己就写在 `#app`
  里面，于是整个界面连同向导一起被冻住：鼠标点不到任何元素，Tab 也进不去。
  但 JS 直接 `element.click()` 仍然有效、控制台零报错，所以看起来像程序卡死，
  或者被当成用户的机器有问题。现在只给 `#app` 里除向导与提示条之外的子节点设 `inert`。
  发布前发现并修掉，首个版本的产物哈希已包含此修复（回归断言：`tools/quality_check.py`【29】）。

- 自检会破坏用户数据。`run.py --selftest` 过去会把已设置的壁纸当作「默认值」并在结束时删掉，
  在已有壁纸的机器上必然报失败。现在先记录原壁纸，结束时按字节原样还原。
- 配置文件被 BOM 静默重置。外部编辑器写出的自带 BOM 的 `config.json`，
  比如 PowerShell 5.1 的 `Set-Content -Encoding UTF8`，过去会被判为损坏并静默重置为默认值，
  连已保存的模型 Key 一起丢掉。现在按 `utf-8-sig` 读取，并保留一份 `config.json.bak` 供恢复。
- 表格抽取串表。相邻两张表格之间没有空行时，下一张表的表头会被当成上一张表的数据行，
  产出以列名为标题、置信度 0.9 的垃圾条目。现在按表头切分，切片后的每一片也都带着自己的表头。
- 畸形 URL 中断整轮裁决。单条形如 `http://[abc` 的地址过去会让跨来源一致性裁决整体抛异常。
- 玩一玩角色页改模板时产出错数字。缺少「简介」结束标记时，过去会退化成在整页正文里找
  「生命值…数字」，把「生命值上限提高 20%」记成「初始生命 = 20」。现在该页直接判解析失败，
  并在丢弃原因里说明是结束标记缺失。
- `kb.min_relevance` 与 `answer.cite_sources` 未生效。这两项设置过去读取正常，但从未参与行为。
- 流式回答不重试。单次 429/5xx 过去会直接中断流式请求，重试次数对流式无效；
  错误里也不带状态码，界面无法区分「Key 失效」与「服务端过载」。
- 关掉自动更新后仍会抓取。总开关 `auto_update.enabled` 过去不影响启动时的那一轮更新。
- 重定向落点复查。跳转到来源黑名单域名、robots.txt 禁止抓取的位置或本机 / 内网地址时丢弃正文，
  避免被污染的搜索结果把请求打到本机服务。
- 抓取与接口健壮性。Wiki 接口返回风控桩 JSON 时不再缓存 7 天并静默失效；响应超限不再先整包
  读进内存，并按 `max_bytes` 标记截断；`_bad_response_*.txt` 计入缓存上限；超过 2×TTL 的缓存会被清扫。
- 冷却期内的站点不再被多打一次。域名冷却判定移到 `robots.txt` 检查之前：既然已经决定避让某站点，
  就不该再为它读一次 `/robots.txt`，那同样是一次请求；读不到时还会用 fail-closed 文案盖住冷却提示。
- 拒绝打开知识库时不再漏掉数据库连接。库里记录的结构版本比程序新时构造会抛异常，
  而这条连接过去没人关闭，文件句柄会挂到进程退出，在 Windows 上还会锁住 `.db`，
  连删除所在目录都会失败。
- 对话接口上限：提问 ≤ 4000 字、历史 ≤ 12 轮、`top_k` ≤ 50，超限返回 422，不会把服务打成 500。
- 开源治理文件：`DATA_LICENSE.md` 写明内置资料的收录规则与移除流程，另有 `CONTRIBUTING.md`、
  `SECURITY.md`、`CODE_OF_CONDUCT.md`、`THIRD_PARTY_NOTICES.md` 逐依赖许可表、`.github/` 模板与 CI。
- `docs/data_sources_cn.md` 里九游（9game）的写法自相矛盾：页首已写明该来源因 AI 生成的事实污染
  整体移除，优先级表却仍在建议把它当攻略来源入库。现在统一为「不收录；若将来重新评估，先按
  `quality: low` 标记并逐条交叉校验」。
